swagger: "2.0"
info:
  title: Test API
  version: "1.0.0"
  description: A clean Swagger 2.0 fixture used to prove the ruleset passes valid specs.
host: api.example.com
schemes: [https]
paths:
  /things:
    get:
      operationId: getThings
      summary: Get things
      description: Returns a list of things.
      tags: [things]
      responses:
        '200':
          description: OK
securityDefinitions:
  apiKeyAuth:
    type: apiKey
    in: header
    name: X-API-Key
tags:
  - name: things
